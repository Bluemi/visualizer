#ifndef __TYPES_CLASS__
#define __TYPES_CLASS__

#include <cstddef>

namespace visualizer {
	/**
	 * Type to define number of vertices.
	 */
	typedef size_t n_vertices;

	/**
	 * Type to define number of floats.
	 */
	typedef size_t n_floats;

	/**
	 * Type to define number of triangles.
	 */
	typedef size_t n_triangles;
}

#endif
